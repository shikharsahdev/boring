A labelled text field, 56px tall by default so it reads and taps well at arm's length.

```jsx
<Input label="Parent's mobile number" help="We send lab timings here." inputMode="tel" />
<Input label="Pincode" note="We don't collect at this pincode yet." invalid />
```

Validation copy explains and offers a way forward; it never blames. Amber, never red.
