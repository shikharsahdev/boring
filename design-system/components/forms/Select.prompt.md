Use for closed lists of 5+ options (cities, relationships, time slots). Below that, prefer `Radio` rows — they are easier to tap.

```jsx
<Select label="Who is this plan for?" options={["Mother", "Father", "Both parents"]} />
```
