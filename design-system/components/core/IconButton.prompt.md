A single-glyph tap target for headers, list rows and sheets; always pass `label`.

```jsx
<IconButton name="chevron-left" label="Back" />
<IconButton name="download" label="Download report" variant="filled" />
```

`md` is 48px, `lg` is 56px. Use `filled` when it sits on paper with no other affordance nearby.
